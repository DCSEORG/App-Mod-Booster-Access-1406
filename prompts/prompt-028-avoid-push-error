RULES — follow these for the entire session without exception:
- NEVER run "git push" in the bash tool. It will always fail.
- NEVER run "git commit" in the bash tool and then try to push separately.
- ALWAYS use the report_progress tool to stage, commit, and push changes.
