@page "{id:int}"
@model NorthwindApp.Pages.Orders.EditModel
@{ ViewData["Title"] = "Edit Order"; }

<div class="page-header">
    <div class="d-flex justify-content-between align-items-center">
        <h1><i class="bi bi-pencil me-2"></i>Edit Order #@Model.Order.OrderID</h1>
        <a asp-page="Index" class="btn btn-outline-secondary"><i class="bi bi-arrow-left me-1"></i>Back</a>
    </div>
</div>
<div class="px-4">
    <div class="card" style="max-width: 600px;">
        <div class="card-body">
            <form method="post">
                <input type="hidden" asp-for="Order.OrderID" />
                <div class="row g-3">
                    <div class="col-12">
                        <label class="form-label fw-bold">Customer *</label>
                        <select asp-for="Order.CustomerID" class="form-select" required>
                            @foreach (var c in Model.Customers)
                            {
                                <option value="@c.CustomerID">@c.CustomerName</option>
                            }
                        </select>
                    </div>
                    <div class="col-12">
                        <label class="form-label">Employee</label>
                        <select asp-for="Order.EmployeeID" class="form-select">
                            <option value="">-- None --</option>
                            @foreach (var e in Model.Employees)
                            {
                                <option value="@e.EmployeeID">@e.FullNameFNLN</option>
                            }
                        </select>
                    </div>
                    <div class="col-md-4">
                        <label class="form-label">Order Date</label>
                        <input asp-for="Order.OrderDate" type="date" class="form-control" />
                    </div>
                    <div class="col-md-4">
                        <label class="form-label">Shipped Date</label>
                        <input asp-for="Order.ShippedDate" type="date" class="form-control" />
                    </div>
                    <div class="col-md-4">
                        <label class="form-label">Paid Date</label>
                        <input asp-for="Order.PaidDate" type="date" class="form-control" />
                    </div>
                    <div class="col-12">
                        <label class="form-label">Status</label>
                        <select asp-for="Order.StatusID" class="form-select">
                            <option value="">-- Status --</option>
                            @foreach (var s in Model.Statuses)
                            {
                                <option value="@s.StatusID">@s.StatusName</option>
                            }
                        </select>
                    </div>
                    <div class="col-12"><label class="form-label">Notes</label><textarea asp-for="Order.Notes" class="form-control" rows="3"></textarea></div>
                    <div class="col-12"><button type="submit" class="btn btn-primary me-2">Save</button><a asp-page="Index" class="btn btn-outline-secondary">Cancel</a></div>
                </div>
            </form>
        </div>
    </div>
</div>
